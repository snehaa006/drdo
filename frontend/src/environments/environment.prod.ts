export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
  mapConfig: {
    defaultCenter: [78.9629, 20.5937] as [number, number],
    defaultZoom: 6,
    minZoom: 3,
    maxZoom: 18
  }
};
